# --- Server ---
PORT=5000

# --- Local MongoDB (Docker) ---
# authSource=admin forces Mongo to check the admin DB for the root user
MONGODB_URI=mongodb://admin:secret@localhost:27017/smart_expense?authSource=admin

# --- Local Redis (Docker) ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=        # leave blank → no password

# --- JWT secrets (make up long random strings) ---
JWT_ACCESS_SECRET=change_me_access
JWT_REFRESH_SECRET=change_me_refresh